<Window x:Class="OverlayTranslate.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:loc="clr-namespace:OverlayTranslate.Localization"
        Title="{loc:Loc Settings_Title}" Width="520" Height="620"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        Style="{DynamicResource ThemedWindowStyle}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl chrome:WindowChrome.IsHitTestVisibleInChrome="True">
            <!-- OCR 设置 -->
            <TabItem Header="{loc:Loc Settings_OcrTab}">
                <StackPanel Margin="12">
                    <TextBlock Text="{loc:Loc Settings_DefaultOcrEngine}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding OcrEngineNames}" SelectedItem="{Binding SelectedOcrEngine}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Engine_PaddleOCR}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="{loc:Loc Settings_ModelPath}" Width="80" VerticalAlignment="Center" />
                        <TextBox Text="{Binding PaddleModelPath}" Width="280" />
                    </StackPanel>

                    <TextBlock Text="{loc:Loc Settings_RemoteOcr}" FontWeight="SemiBold" Margin="0,8,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="{loc:Loc Settings_EndpointUrl}" Width="80" VerticalAlignment="Center" />
                        <TextBox Text="{Binding RemoteOcrEndpoint}" Width="280" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="API Key:" Width="80" VerticalAlignment="Center" />
                        <TextBox Text="{Binding RemoteOcrApiKey}" Width="280" />
                    </StackPanel>

                    <TextBlock Text="{loc:Loc Settings_FallbackEngine}" FontWeight="SemiBold" Margin="0,8,0,4" />
                    <ComboBox ItemsSource="{Binding FallbackOptions}" SelectedItem="{Binding SelectedOcrFallback}" Width="200" HorizontalAlignment="Left" />
                </StackPanel>
            </TabItem>

            <!-- 翻译设置 -->
            <TabItem Header="{loc:Loc Settings_TranslationTab}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="{loc:Loc Settings_DefaultTranslationEngine}" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <ComboBox ItemsSource="{Binding TranslationEngineNames}" SelectedItem="{Binding SelectedTranslationEngine}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                        <TextBlock Text="{loc:Loc Engine_DeepL}" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="API Key:" Width="80" VerticalAlignment="Center" />
                            <TextBox Text="{Binding DeepLApiKey}" Width="280" />
                        </StackPanel>
                        <CheckBox IsChecked="{Binding DeepLFreeTier}" Content="{loc:Loc Settings_FreeTier}" Margin="80,0,0,12" />

                        <TextBlock Text="{loc:Loc Settings_BaiduTranslation}" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="App ID:" Width="80" VerticalAlignment="Center" />
                            <TextBox Text="{Binding BaiduAppId}" Width="280" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="{loc:Loc Settings_Secret}" Width="80" VerticalAlignment="Center" />
                            <TextBox Text="{Binding BaiduSecret}" Width="280" />
                        </StackPanel>

                        <TextBlock Text="{loc:Loc Engine_OpenAI}" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="API Key:" Width="80" VerticalAlignment="Center" />
                            <TextBox Text="{Binding OpenAiApiKey}" Width="280" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="{loc:Loc Settings_Model}" Width="80" VerticalAlignment="Center" />
                            <TextBox Text="{Binding OpenAiModel}" Width="280" />
                        </StackPanel>

                        <TextBlock Text="{loc:Loc Settings_MicrosoftTranslation}" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBlock Text="{loc:Loc Settings_NoConfigNeeded}" FontSize="11" Margin="80,0,0,12" />
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- 语言设置 -->
            <TabItem Header="{loc:Loc Settings_LanguageTab}">
                <StackPanel Margin="12">
                    <TextBlock Text="{loc:Loc Settings_DefaultSourceLang}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SourceLanguage}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_DefaultTargetLang}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding TargetLanguage}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />
                </StackPanel>
            </TabItem>

            <!-- 热键设置 -->
            <TabItem Header="{loc:Loc Settings_HotkeyTab}">
                <StackPanel Margin="12">
                    <TextBlock Text="{loc:Loc Settings_ScreenshotHotkey}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <CheckBox IsChecked="{Binding HotkeyCtrl}" Content="Ctrl" Margin="0,0,8,0" />
                        <CheckBox IsChecked="{Binding HotkeyAlt}" Content="Alt" Margin="0,0,8,0" />
                        <CheckBox IsChecked="{Binding HotkeyShift}" Content="Shift" Margin="0,0,8,0" />
                        <TextBlock Text="+" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Text="{Binding HotkeyKey}" Width="40" />
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <!-- 其他设置 -->
            <TabItem Header="{loc:Loc Settings_OtherTab}">
                <StackPanel Margin="12">
                    <TextBlock Text="{loc:Loc Settings_LogLevel}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding LogLevels}" SelectedItem="{Binding SelectedLogLevel}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_LogFilePath}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding LogFile}" Width="360" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_PythonRuntimePath}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding PythonRuntimePath}" Width="360" HorizontalAlignment="Left" Margin="0,0,0,12" />
                    <TextBlock Text="{loc:Loc Settings_PythonHint}" FontSize="11" />

                    <TextBlock Text="{loc:Loc Settings_Theme}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_UiLanguage}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding LocaleOptions}" SelectedItem="{Binding SelectedLocale}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_FontSizeMode}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox ItemsSource="{Binding FontModes}" SelectedItem="{Binding SelectedFontMode}" Width="200" HorizontalAlignment="Left" Margin="0,0,0,12" />

                    <TextBlock Text="{loc:Loc Settings_CustomFontSize}" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox Text="{Binding CustomFontSize}" Width="100" HorizontalAlignment="Left" Margin="0,0,0,12" />
                </StackPanel>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{loc:Loc Settings_Save}" Width="80" Height="30" Click="OnSaveClick" Margin="0,0,8,0" />
            <Button Content="{loc:Loc Settings_Cancel}" Width="80" Height="30" Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
